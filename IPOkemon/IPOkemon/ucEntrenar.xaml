<UserControl
    x:Class="IPOkemon.ucEntrenar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IPOkemon"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="auto"
    Width="150">

    <Grid Width="auto" Height="auto">
        <Border Background="Black" BorderBrush="#4d4c4b" BorderThickness="1" Opacity="0.2" Width="auto" Height="auto"/>
        <StackPanel Orientation="Vertical" Spacing="5" HorizontalAlignment="Stretch" Width="auto" Height="auto">
            <Image x:Name="imgPokemon" Height="auto" Width="auto" Stretch="Fill" Source="{Binding sprite}"/>
            <StackPanel Orientation="Horizontal" Spacing="15" HorizontalAlignment="Center" Width="auto" Height="auto">
                <TextBlock x:Name="txtNombre" Text="{Binding nombre}" Width="auto"/>
                <TextBlock x:Name="txtNivel" Width="auto"/>
            </StackPanel>
            <ProgressBar x:Name="pbExp" Height="10" Value="{Binding exp}" Margin="5,0,5,0" HorizontalAlignment="Stretch" Width="auto"/>
            <TextBlock x:Name="txtExpRestante" Width="auto" HorizontalAlignment="Center" Height="auto"/>
        </StackPanel>
    </Grid>
</UserControl>
