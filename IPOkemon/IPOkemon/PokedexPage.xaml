<Page
    x:Class="IPOkemon.PokedexPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IPOkemon"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="103*"/>
            <ColumnDefinition Width="147*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="153*"/>
            <RowDefinition Height="847*"/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Left" Margin="29,0,0,0" Text="Filtrar por:" TextWrapping="Wrap" VerticalAlignment="Center" Height="37" Width="200" FontSize="22"/>
            <ComboBox x:Name="comboTypes" Width="342" VerticalAlignment="Center" Height="58" Margin="236,0,0,0"/>
        <AutoSuggestBox x:Name="buscador" TextChanged="AutoSuggestBox_TextChanged" CornerRadius="30"
                SuggestionChosen="AutoSuggestBox_SuggestionChosen"
                Width="774" AutomationProperties.Name="Basic AutoSuggestBox" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Height="59" QueryIcon="Find" PlaceholderText="Busca tu pokemon..." FontSize="22" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>

        <GridView ItemsSource="{x:Bind pokemons}" IsItemClickEnabled="True" Grid.ColumnSpan="2" Grid.Row="1">
            <GridView.ItemTemplate>
                <DataTemplate x:Name="templatePokedex" x:DataType="local:Pokemon">
                    <Grid Name="gridelemento" Height="175" Width="200">
                        <Image x:Name="imagePokemon" Source="{x:Bind Path=sprite}" Stretch="UniformToFill" HorizontalAlignment="Left"/>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                            <TextBlock Text="{x:Bind nombre}" FontSize=""/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>

    </Grid>
</Page>
